import React from 'react';
import AppRoutes from './routes/AppRoutes';

/**
 * Componente principal que define las rutas de la aplicación. Utiliza
 * react-router-dom para crear una SPA con rutas protegidas.
 */
const App = () => {
  return <AppRoutes />;
};

export default App;
